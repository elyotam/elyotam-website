# ElyotamWebsite

אתר התדמית של Elyotam, בגרסת הצילום הריאליסטי. עמוד אחד, עברית RTL, בלי build step.
הקוד זהה ל-`army-website`, רק הרצף והתזמון הוחלפו.

## הרצה מקומית

```bash
python -m http.server 8092
# http://127.0.0.1:8092
```

חייב לרוץ מעל HTTP ולא מ-`file://`, כי `main.js` הוא ES module.

## מבנה התיקייה

| נתיב | מה יש בו |
| --- | --- |
| `index.html` | המבנה והקופי |
| `styles.css` | העיצוב |
| `main.js` | הגלילה, התזמון של הכותרות, האנימציות |
| `hero-frames.js` | נגן הפריימים שמצייר את ה-hero על canvas |
| `field.css` | העיצוב של כל מה שמתחת לסרט ושל דף הקשר: זית, חול וכתום אות |
| `contact.html` | החמ״ל: דף יצירת הקשר עם הטופס |
| `contact.js` | שליחת הטופס למייל דרך FormSubmit, והעברת אותם פרטים לוואטסאפ |
| `i18n.js` | כל התרגום לאנגלית של שני הדפים, ומתג HE/EN |
| `lang.css` | עיצוב המתג, והשיקוף של הכללים שנכתבו לימין-לשמאל כשהדף באנגלית |
| `frames/` | 1175 פריימים למחשב, 1344x768 |
| `frames-mobile/` | 1175 פריימים לטלפון, חיתוך אנכי מהמרכז, 432x768 |
| `source/clips/` | 8 הקטעים המקוריים, `1.mp4` עד `8.mp4`. לא ב-git |
| `source/clips-old-rockets/` | קטעים 6 ו-7 הקודמים (משגר הרקטות), גיבוי בלבד. לא ב-git |
| `reveal-frames/` | 158 פריימים של סצנת ההאנגר שפותחת את "תיק מבצעים", 1344x768 |
| `reveal-frames-mobile/` | אותם 158 פריימים לטלפון, 432x768 |
| `projects/` | צילומי המסך של הפרויקטים בתיק המבצעים (מחשב וטלפון לכל אחד) |
| `source/clips/reveal-9.mp4` | קטע ההאנגר. לא ב-git |
| `source/hero.mp4` | כל הקטעים מחוברים לסרט אחד. לא ב-git |

## איך נבנו הפריימים

קטעים 1 עד 5: 158 פריימים, 24fps, 1344x768. קטעים 6 ו-7 (תקיפת ה-F-35): 121 פריימים,
864x496, מוגדלים ל-1344x768. קטע 8 (הנחיתה): 150 פריימים ב-30fps, 1284x716, מוגדל לגובה 768
וחתוך למרכז ל-1344x768 בלי מתיחה. כל הפריימים נשמרים. כל קטע מתחיל בדיוק בפריים שהקודם נגמר
בו, אז מקטעים 2 עד 8 נזרק הפריים הראשון: `158 + 4 x 157 + 2 x 120 + 149 = 1175`.

```bash
ffmpeg -i 1.mp4 ... -i 8.mp4 -filter_complex   "[0:v]setpts=N/24/TB[v0];[1:v]trim=start_frame=1,setpts=N/24/TB[v1];...
   [5:v]scale=1344:768:flags=lanczos,trim=start_frame=1,setpts=N/24/TB,setsar=1[v5];
   [6:v]scale=1344:768:flags=lanczos,trim=start_frame=1,setpts=N/24/TB,setsar=1[v6];
   [7:v]scale=-2:768:flags=lanczos,crop=1344:768,trim=start_frame=1,setpts=N/24/TB,setsar=1[v7];
   concat=n=8:v=1:a=0,split=2[d][m];[m]crop=ih*9/16:ih[mc]"   -map "[d]"  -fps_mode passthrough -c:v libwebp -quality 82 frames/frame_%04d.webp   -map "[mc]" -fps_mode passthrough -c:v libwebp -quality 80 frames-mobile/frame_%04d.webp
```

`-fps_mode passthrough` חובה. בלעדיו ffmpeg משכפל פריימים בחיבורים והגלילה נתקעת בכל מעבר.
כשמחליפים פריימים, הפקודה דורסת את 1 עד N, ואז מוחקים כל קובץ ישן עם מספר גבוה יותר.

## התזמון

התזמון כתוב בפריימים. ב-`main.js`: `FRAMES = 1175`, `FILM_END = 0.94`, ו-`at(frame)` הופך מספר
פריים למיקום בציר הזמן (ו-`frameAt` עושה את ההפך). הסרט מגיע לפריים האחרון ב-94% מהגלילה
ונעצר עליו עד הסוף. `SLOW` מגדיר אזורים שבהם כל פריים מקבל יותר גלילה: פריימים 1008–1068
(הטיפוס מעל העננים בזריחה) רצים פי 2.2 לאט, כמו הילוך איטי, כדי ש"עליונות אווירית" תספיק
לנחות ולהיקרא. אורך הגלילה גדל יחד עם הסרט (7.2 מסכים, 5.5 בטלפון), כך שכל שאר הסצנות שומרות
על אותו קצב.

| פריימים | מה בפריים | מה מופיע |
| --- | --- | --- |
| 0 – 55 | בטן המטוס, הרמפה פתוחה | שעת אפס · זה לא עוד אתר. זה מבצע. (מוקלד, נועל גלילה פעם אחת בלשונית) |
| 88 – 189 | נפילה חופשית מעל העננים | חנויות דרופשיפינג · דרופ מדויק · מהספק ישר ללקוח. בלי מחסן ובלי מלאי! |
| 244 – 346 | מסוקים מעל העיר | ELYOTAM, וחמשת השירותים מתחת |
| 412 – 511 | טנק ברחוב, חיילים מאחוריו | אתרי תדמית · פורצים קדימה · אתר שמציב את העסק בחזית. |
| 555 – 648 | החדר והכוונת של הצלף | דפי נחיתה · בול פגיעה · דף אחד, מטרה אחת. |
| 656 – 695 | דרך הכוונת לפיצוץ, ואבק | "לקוח" לבד, גדול, ננעל במרכז הפיצוץ |
| 712 – 803 | חוליה לאורך קיר, רחפנים מעל | אוטומציות AI · חוליה שלא ישנה · סוכני AI שמטפלים בכל פנייה, מסביב לשעון. |
| 804 – 1009 | שני F-35 חולפים מעל החוליה, תא החימוש נפתח, הפצצות משתחררות, פיצוץ, עשן, טיפוס | (בלי כיתוב) |
| 1010 – 1066 | שני ה-F-35 עולים מהעשן מעל העננים בזריחה (בהילוך איטי) | פיתוח אפליקציות · עליונות אווירית · כשהאפליקציה שלך באוויר, למתחרים אין שמיים. |
| 1067 – 1143 | צלילה דרך העננים למדבר, ונחיתה | (בלי כיתוב) |
| 1144 – 1174 | המטוסים נעצרים מול ההאנגר, צוות הקרקע מתקרב | המבצע שלך מתחיל בשיחה אחת. (נשאר עד סוף הגלילה) |

לפני שמזיזים תזמון, פותחים את `frames/frame_XXXX.webp` (מספר הקובץ = מספר הפריים ועוד 1)
ומוודאים שהסצנה מתאימה.

## סצנת ההאנגר ותיק המבצעים

"04 · תיק מבצעים" נפתח בסצנה שנייה שרצה בגלילה, נפרדת מהסרט של הפתיח: 158 פריימים של האנגר
סגור שהדלתות שלו נפתחות וחושפות F-35. `initHangarReveal()` ב-`main.js` נועל את הבמה לאורך 2.6
מסכים (2.2 בטלפון). הדלתות רצות ב-80% הראשונים, הכותרת "מבצעים שהושלמו." עולה ב-62%, ובסוף
המטוס נשאר חשוף מתחת לכותרת. `hero-frames.js` מקבל תיקייה ומספר פריימים משלו לכל סצנה.

אחרי הסצנה באים תיקי המבצעים: 01 (משרד משה שטרן) ברוחב מלא, ו-02 עד 05 (אהובית מור, Code92,
Army Website, הפורטפוליו האישי) שניים בשורה. כל תיק כולל צילום מסך למחשב ולטלפון (`projects/`),
תיאור ושלוש עובדות שנכתבו רק ממה שקיים באתר עצמו, וכפתור לאתר החי. לא מוסיפים פרויקט בלי קישור
אמיתי ובלי אישור של הבעלים.

## קופי

שפה מבצעית (שעת אפס, שולטים בשטח, יחידת צלפים, עליונות אווירית, חוליה שלא ישנה, דרופ מדויק, פקודת מבצע, קשר לחמ״ל). כל כותרת
מצביעה על מה שבפריים ואומרת במדויק מה השירות עושה. השירותים, בשמות שלהם בדיוק: אתר תדמית,
דפי נחיתה, פיתוח אפליקציות, אוטומציות AI, חנויות E-commerce ו-Dropshipping.
שמות לטיניים בתוך משפט עברי (`App Store`, `Google Play`) עטופים ב-`<bdi>`, אחרת סדר המילים
והנקודה מתהפכים.

## סימן החיסול על הפיצוץ

כרטיס דפי הנחיתה נגמר ב"דף אחד, מטרה אחת:", והמילה "לקוח" מופיעה לבד ב-`#zone-target`, במרכז
המסך, בדיוק כשהמטען מתפוצץ. `targetLockTween()` ב-`main.js` מריץ את הרצף על ה-timeline של הגלילה
לפי `TARGET = { in: 0.597, out: 0.632 }`:

1. כוונת צלף (SVG: עיגול, טבעת מקווקוות שמסתובבת, קווי כוונת) נכנסת מסתובבת מרחוק ונועלת על המילה.
2. **פגיעה** ב-`in + 0.01`, על שיא הפיצוץ: ה-X של סימן הפגיעה מכה, הבזק כתום, והכוונת והסימן רועדים.
3. חותמת "המטרה הושגה" נוחתת מתחת למילה, מוטה.

גלילה אחורה מריצה את הכל הפוך. הזרועות של ה-X בנויות כ-`<i>` שמחזיק את הזווית ו-`<b>` שמונפש,
כדי שה-tween לא ידרוס את הסיבוב. בכוונה החותמת היא "המטרה הושגה" ולא "חוסל", כי "לקוח חוסל" נקרא לא נכון.

## העיצוב מתחת לסרט

`field.css` נטען אחרי `styles.css` ודורס רק את מה שמתחת ל-hero, ואת שורת הסיום שמעבירה אליו.
רקעים בגווני זית, כיתוב בצבע חול, וכתום אחד (`--signal`) לכל מה שמבקש פעולה. מפת קווי גובה עדינה
ברקע, תגיות סקשן כחותמת, כותרות ב-Karantina, כרטיסים עם פינות כוונת, וכפתורים עם פינות חתוכות.

## החמ״ל: דף הקשר

`contact.html` מקושר מהכפתור "לתיאום תדריך", מ"קשר לחמ״ל" בסיום ומהפוטר. הטופס:

- **מייל:** `contact.js` שולח ל-`https://formsubmit.co/ajax/elyotam.finance@gmail.com`.
  **בפעם הראשונה** FormSubmit שולח מייל אימות לתיבה הזו, ושום פנייה לא נמסרת עד שלוחצים על
  הקישור שבו. אחרי האימות FormSubmit נותן כתובת מוסווית; כדאי להחליף אותה ב-`ENDPOINT` כדי שהמייל
  לא יופיע בקוד של הדף.
- **וואטסאפ:** "שליחה בוואטסאפ" פותח את 052-2057074 עם כל הפרטים כבר כתובים בהודעה.
- חובה: שם, טלפון, מייל, בחירה של לפחות תחום אחד, ותדריך. רק שם העסק לא חובה. אותה בדיקה רצה גם
  לפני שליחה בוואטסאפ. שדה נסתר (`_honey`) עוצר בוטים. אם השליחה למייל נכשלת, מוצגת הפניה לוואטסאפ.

## עברית / אנגלית

ה-HTML נשאר בעברית. `i18n.js` רץ בסוף ה-`<body>`, לפני `main.js`, ובאנגלית מחליף את התוכן של כל
אלמנט ברשימה שלו (סלקטור ← HTML), מחליף מאפיינים (placeholder, aria-label, meta) והופך את הדף ל-LTR.
הוא רץ לפני שה-hero מפצל אותיות ומילים, ולכן כל האנימציות עובדות על הטקסט באנגלית.

- **הבחירה:** קודם `?lang=en` / `?lang=he` בכתובת, אחר כך הבחירה האחרונה ששמורה ב-localStorage, ואחרת עברית.
  לחיצה על המתג שומרת את הבחירה וטוענת את הדף מחדש.
- **מניעת הבהוב:** סקריפט קטן ב-`<head>` מסמן `i18n-pending` כשהשפה אנגלית, והגוף מוסתר עד שהתרגום הוחל.
- **מחרוזות בתוך סקריפטים:** `window.t(key, hebrew)`. תוויות הפס הצדדי ב-`main.js`, והודעות הטופס,
  הודעת הוואטסאפ ונושא המייל ב-`contact.js`.
- **הוספת טקסט חדש לאתר:** מוסיפים שורה ל-`INDEX.text` או `CONTACT.text` ב-`i18n.js`, אחרת הוא יישאר בעברית גם במצב אנגלית.
- **אורכים:** באנגלית כותרות הסרט קטנות יותר, וה-lead והשורות בסיום קטנים יותר בטלפון, כדי שכל
  `.reveal-line` יישאר בשורה אחת. נבדק ב-320 עד 1600.

## מה עוד צריך לפני שעולים לאוויר

- **הסטטיסטיקות** הן עובדות שנובעות מההצעה עצמה (24/7, 5 כוחות, איש קשר אחד). אם יש מספרים
  אמיתיים, כמו כמות פרויקטים שעלו, אפשר להחליף.
- **אימות FormSubmit.** לשלוח פנייה אחת מהאתר ולאשר את המייל שמגיע, אחרת פניות לא יגיעו.
- **פרטים בתוך הסרט.** בסצנת הצלף תלויים בדים ירוקים-לבנים שנראים כמו דגלים, ועל דלת
  משאית המשגר יש כוכב לבן. כדאי להחליט אם זה בסדר לאתר עסקי, או לייצר את שני הקטעים מחדש.
